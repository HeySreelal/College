#include <stdio.h>

int main() {
    // Problem: Sum of two numbers.
    
    int a, b;
    printf("Enter the two numbers: ");
    scanf("%d %d", &a, &b);
    printf("Sum of %d and %d is %d\n", a, b, a + b);
    return 0;
}