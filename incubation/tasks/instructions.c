/*
    Amstrong Number
    ----------------
    A number is called an amstrong number if the sum of the cubes of its digits is equal to the number itself.
    For example, 371 is an amstrong number because 3^3 + 7^3 + 1^3 = 371.

    Task:
    Write a program to check if a number is an amstrong number.

    Input Format:
        An integer number.

    Output Format:
        Print "Yes" if N is an amstrong number otherwise print "No".

*/

#include <stdio.h>

int main() {

    int n;
    
    printf("Enter a number: ");
    scanf("%d", &n);

    // Complete the code.

    return 0;
}